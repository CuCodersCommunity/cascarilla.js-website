---
import { Scroll } from "lucide-astro";
---

<section class="px-4 py-20" id="get-started">
  <div class="container mx-auto max-w-6xl">
    <div class="mb-12 text-center">
      <h2 class="mb-4 text-3xl font-bold tracking-tight sm:text-4xl">
        <span
          class="bg-gradient-to-r from-purple-400 to-pink-400 bg-clip-text text-transparent"
        >
          Getting Started
        </span>
      </h2>
      <p class="mx-auto max-w-2xl text-lg text-purple-200/70">
        Begin your magical journey with Cascarilla. Follow these enchanted steps
        to harness its power.
      </p>
    </div>

    <div class="grid gap-8 lg:grid-cols-2 ">
      {/* Installation */}
      <div
        class="rounded-2xl border border-purple-500/20 bg-purple-500/[0.02] p-6"
      >
        <h3 class="mb-4 text-xl font-semibold text-purple-200">Installation</h3>
        <p class="mb-4 text-purple-200/70">
          Install Cascarilla using your preferred package manager:
        </p>
        <div class="mb-6 space-y-4">
          <div class="rounded-lg bg-black/50 p-4">
            <div class="flex items-center justify-between">
              <code class="font-mono text-pink-400">npm install cascarilla</code
              >
              <button
                class="ml-2 rounded-md p-1 text-purple-400 hover:bg-purple-500/10 hover:text-purple-300"
                onclick="navigator.clipboard.writeText('npm install cascarilla')"
              >
                <Scroll class="h-4 w-4" />
                <span class="sr-only">Copy to clipboard</span>
              </button>
            </div>
          </div>
          <div class="rounded-lg bg-black/50 p-4">
            <div class="flex items-center justify-between">
              <code class="font-mono text-pink-400">pnpm add cascarilla</code>
              <button
                class="ml-2 rounded-md p-1 text-purple-400 hover:bg-purple-500/10 hover:text-purple-300"
                onclick="navigator.clipboard.writeText('pnpm add cascarilla')"
              >
                <Scroll class="h-4 w-4" />
                <span class="sr-only">Copy to clipboard</span>
              </button>
            </div>
          </div>
          <div class="rounded-lg bg-black/50 p-4">
            <div class="flex items-center justify-between">
              <code class="font-mono text-pink-400">yarn add cascarilla</code>
              <button
                class="ml-2 rounded-md p-1 text-purple-400 hover:bg-purple-500/10 hover:text-purple-300"
                onclick="navigator.clipboard.writeText('yarn add cascarilla')"
              >
                <Scroll class="h-4 w-4" />
                <span class="sr-only">Copy to clipboard</span>
              </button>
            </div>
          </div>
        </div>
      </div>

      {/* Usage */}
      <div
        class="rounded-2xl border border-purple-500/20 bg-purple-500/[0.02] p-6"
      >
        <h3 class="mb-4 text-xl font-semibold text-purple-200">Usage</h3>
        <p class="mb-4 text-purple-200/70">
          Import and use Cascarilla magical functions in your application:
        </p>
        <div class="rounded-lg bg-black/50 p-4">
          <div class="flex items-center justify-between">
            <code class="font-mono text-pink-400">
              <span class="text-purple-400">import</span>
              {"{ siacaraa }"}{" "}
              <span class="text-purple-400">from</span>
              <span class="text-green-400">'cascarilla'</span>;
            </code>
            <button
              class="ml-2 rounded-md p-1 text-purple-400 hover:bg-purple-500/10 hover:text-purple-300"
              id="copy-import"
            >
              <Scroll class="h-4 w-4" />
              <span class="sr-only">Copy to clipboard</span>
            </button>
          </div>
        </div>

        <div class="mt-4 rounded-lg bg-black/50 p-4">
          <code>
            <span class="text-purple-400"
              >// Transform your data with magical functions</span
            >
            <span class="text-purple-300">siacaraa();</span>
          </code>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  const copyImportButton = document.querySelector("#copy-import");
  copyImportButton?.addEventListener("click", () => {
    navigator.clipboard.writeText('import { siacaraa } from "cascarilla";');
  });
</script>
